{
  "name": "sakila-example",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "node ./bin/www & firefox --new-window http://localhost:3000/",
    "install": "cd client && npm install && npm run-script build && cd ../database && gnome-terminal -e \"bash -c 'sudo sh mongo-sakila.sh'\"  && cd .."
  },
  "dependencies": {
    "cookie-parser": "~1.4.3",
    "debug": "~2.6.9",
    "express": "~4.16.0",
    "morgan": "~1.9.0",
    "mongodb": "^3.2.3"
  }
}
